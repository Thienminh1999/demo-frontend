import { ColorDefaultEnum } from "@/config/colors.constant";
import styled from "styled-components";

export const Container = styled.div`
    padding: 1.5rem;
    background-color: ${ColorDefaultEnum.ONE_THEME_COLOR};
    color: white;
    font-size: 2rem;
`